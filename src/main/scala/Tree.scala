object Tree extends App{
//print a tree given some argument (or read it from user input)
  //provide a default of 5
  //3 means rows. Would be nicer to use val instead of var to get the argument
  //  *
  // ***
  //*****

  //number comes from user input and argument is preferable. if no argument is given 3 is default
}
